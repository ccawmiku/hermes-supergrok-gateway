services:
  gateway:
    image: ghcr.io/ccawmiku/hermes-supergrok-gateway:v1.0.4
    container_name: hermes-supergrok-gateway
    restart: unless-stopped
    ports:
      - "8645:8645"
    volumes:
      - supergrok-data:/data

volumes:
  supergrok-data:
